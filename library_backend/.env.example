# 书架后端环境变量示例
# 复制为 .env 后按需修改

LIBRARY_HOST=0.0.0.0
LIBRARY_PORT=5002
LIBRARY_DEBUG=True

# SQLite 数据库路径（默认 library_backend/data/library.db）
# LIBRARY_DB_PATH=/path/to/library.db

# CORS 允许的来源（* 表示全部，生产环境建议限制）
LIBRARY_CORS_ORIGINS=*
